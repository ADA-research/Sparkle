repos:
  -   repo: https://github.com/pycqa/flake8
      rev: '5.0.4'  # pick a git hash / tag to point to
      hooks:
      -   id: flake8
          name: flake8
  # -   repo: local
  #     hooks:
  #     - id: pytest-check
  #       name: pytest-check
  #       stages: [commit]
  #       types: [python]
  #       entry: pytest
  #       language: system
  #       pass_filenames: false
  #       always_run: true
